import * as React from "react";

import { House } from "./../types";

interface ControlsProps {}

export const Controls = (props: ControlsProps) => (
  <div className="controls-container">controls here</div>
);
